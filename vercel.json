{
  "installCommand": "npm install --legacy-peer-deps",
  "regions": ["gru1"],
  "crons": [
    {
      "path": "/api/subscription/expire",
      "schedule": "0 6 * * *"
    }
  ],
  "functions": {
    "app/**/*.tsx": {
      "maxDuration": 30
    }
  }
}
